nie działa:
> przy filtrowaniu po typie dalej wyświetlają się wartości podsumowujące dla wszystkich transakcji

to do:

> trochę więcej danych w modelu
> filtrowanie po datach - wersja jako liczby? czy pobawić się z datami w JS?
> zrobić tak, żeby wielkość liter we wprowadzanych stringach nie miała znaczenia
> CSS żeby to wyglądało jakoś
> zapisywanie danych w local storage